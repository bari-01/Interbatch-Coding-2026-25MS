I put a riscv emulator and ran elf files written in C (intercepting syscalls host-side) on top of WASM of this repo's terminal config (which was if/else react componenents-ish? idk react)

Upcoming: some type of unix kernel (hopefully)
