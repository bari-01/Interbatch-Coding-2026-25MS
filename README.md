I put a riscv emulator and ran elf files written in C (intercepting syscalls host-side) using a network-loaded initramfs (fake virtual file system using linked lists) on top of WASM of this repo's terminal config (which was if/else react componenents-ish? idk react)

Upcoming: some type of unix kernel (hopefully)

(seems like I didn't know make existed, evidently using a compile.sh)
